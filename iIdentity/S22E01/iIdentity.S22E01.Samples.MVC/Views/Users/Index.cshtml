@using iIdentity.S22E01.Samples.MVC.Models.AAA.Data
@*@model List<IdentityUser>*@
@model List<ApplicationUser>

<a class="btn btn-success btn-lg mb-1" asp-controller="Users" asp-action="Create">Create User</a>

<table class="table table-responsive table-light table-bordered table-hover">

    <thead>
        <tr class="table-info">
            @*<th>Id</th>*@
            <th>Username</th>
            <th>Email</th>
            @*<th>Password</th>*@
            <th>SSN</th>
            <th class="text-center">Actions</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Count <= 0)
        {
            <tr>
                <td colspan="4">There is no user in database.</td>
            </tr>
        }
        else
        {
            @foreach (var user in Model)
            {
                <tr>
                    @*<td>@user.Id</td>*@
                    <td>@user.UserName</td>
                    <td>@user.Email</td>
                    @*<td>@user.PasswordHash</td>*@
                    <td>@user.SSN</td>
                    <td class="text-center">
                        @*<form class="d-inline" asp-controller="Users" asp-action="Edit" method="get">
                            <input type="hidden" value="@user.Id" name="id">
                            <input type="submit" value="Edit" class="btn btn-sm btn-primary m-1">
                        </form>*@

                        <a class="btn btn-info btn-sm mb-1" asp-controller="Users" asp-action="Details" asp-route-Id="@user.Id">Details</a>
                        <a class="btn btn-warning btn-sm mb-1" asp-controller="Users" asp-action="Edit" asp-route-Id="@user.Id">Edit</a>
                        <a class="btn btn-danger btn-sm mb-1" asp-controller="Users" asp-action="Delete" asp-route-Id="@user.Id">Delete</a>
                        <a class="btn btn-dark btn-sm mb-1" asp-controller="Users" asp-action="EditUserRoles" asp-route-Id="@user.Id">Edit Roles</a>

                    </td>
                </tr>
            }
        }
    </tbody>
</table>